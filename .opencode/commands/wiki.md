Setup, check, or continue the knowledge vault.

Read WIKI.md in this project first. Then load the wiki skill.

If vault/hot.md does not exist, run first-time setup: create the vault directory structure, seed core files, and ask the user what this vault is for.

If vault/hot.md exists, read it, report the current vault state (page count, recent activity), and suggest next actions.
