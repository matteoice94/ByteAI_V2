Ingest a source into the knowledge vault. $ARGUMENTS

Load the wiki-ingest skill. Read WIKI.md for the schema reference.

Read the source completely. Extract entities and concepts. Create cross-linked wiki pages with proper YAML frontmatter. Update index, hot cache, and log.

Report: pages created, pages updated, key insight added.
